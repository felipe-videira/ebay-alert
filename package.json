{
    "name": "ebay-alert",
    "description": "This package only exists for the purpose of containing scripts.",
    "scripts": {
        "first-start": "docker-compose up --build",
        "start": "docker-compose up",
        "test": "docker-compose -f docker-compose.test.yml up"
    }
}
